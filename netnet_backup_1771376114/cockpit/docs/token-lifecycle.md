# Token lifecycle dashboard

UI: `/token`

API:
- `/api/bankr/token/info`
- `/api/bankr/token/actions`

This is intentionally proposal-only. It exists to:
1) stabilize contracts, 2) produce proof-of-action envelopes, 3) give operators a single place to inspect token ops before execution.
