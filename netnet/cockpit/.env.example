# =============================================================================
# netnet-cockpit Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# x402 Payment Protocol (Required)
# -----------------------------------------------------------------------------
# Your EVM wallet address that will receive x402 payments
# Must be a valid 0x address (42 characters)
X402_PAY_TO=0xYourWalletAddressHere

# In local development only, set true to bypass payment checks entirely.
# Never enable this in production.
# X402_DEV_BYPASS=false

# Optional local authorization token to simulate a paid request.
# If set, sending header "x-netnet-paid-token: <this value>" returns 200.
# Useful for local smoke tests without real x402 settlement.
# X402_DEV_PAID_TOKEN=change-me-local-only

# Blockchain network for payments (optional)
# Default: eip155:84532 (Base Sepolia testnet)
# For mainnet, use: eip155:8453 (Base)
# X402_NETWORK=eip155:84532

# Price in USD for the proof-paid endpoint (optional)
# Default: 0.01
# X402_PRICE_USD=0.01

# x402 facilitator service URL (optional)
# Default: https://x402.org/facilitator
# X402_FACILITATOR_URL=https://x402.org/facilitator

# -----------------------------------------------------------------------------
# External Services (Optional - defaults work for most cases)
# -----------------------------------------------------------------------------
# Bridge.eco API base URL
# Default: https://api.bridge.eco
# BRIDGE_API_BASE_URL=https://api.bridge.eco
# BRIDGE_ECO_API_BASE=https://api.bridge.eco

# Bridge upstream timeout in milliseconds
# Default: 12000
# BRIDGE_TIMEOUT_MS=12000

# Regen MCP endpoint (optional)
# If unset, Regen MCP adapter uses a safe local stub fallback.
# REGEN_MCP_URL=https://your-regen-mcp-endpoint
# REGEN_MCP_API_KEY=

# ecoToken scan dashboard base URL
# Default: https://scan.ecotoken.earth
# ECOTOKEN_SCAN_BASE=https://scan.ecotoken.earth

# -----------------------------------------------------------------------------
# Telegram Operator Bridge (Optional)
# -----------------------------------------------------------------------------
# Telegram bot token used by /api/telegram/webhook to send replies.
TELEGRAM_BOT_TOKEN=

# Shared secret required as ?secret=<value> on Telegram webhook requests.
TELEGRAM_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Operator Wallet Lanes (Privy-managed, Optional)
# -----------------------------------------------------------------------------
# Active wallet profiles in Operator Seat can use these defaults for prompt-only routing.
# These should be Privy-managed server wallet addresses (not personal browser wallets).
PRIVY_WALLET_VEALTH=
PRIVY_WALLET_ECOWEALTH=
